#include "main.h"
#include <stdlib.h>
/**
 * _atoi - a function that convert a string to an integer.
 * @s: string  argument
 * Return: none-zero-value (success)
 */
int _atoi(char *s)
{
	return (atoi(s));
}
