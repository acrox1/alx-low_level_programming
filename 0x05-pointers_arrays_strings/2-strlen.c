#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string.
 * @s: string argument
 * Return: 0 (success)
 */
int _strlen(char *s)
{
	int _slen;

	_slen = strlen(s);

	return (_slen);
}
