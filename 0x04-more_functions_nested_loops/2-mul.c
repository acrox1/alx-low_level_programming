#include "main.h"
#include <ctype.h>
/**
 * mul - a function that checks for lowercase character.
 * @a: is first parameter
 * @b: is second parameter
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}

