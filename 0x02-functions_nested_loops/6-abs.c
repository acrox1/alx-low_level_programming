#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _abs - a function that computes the absolute value of an intege.
 * @x: is the integer to be tested
 * Return: 0 (Success)
 */
int _abs(int x)
{
	int a;

	a = abs(x);
	return (a);

}

